import React, { FC } from 'react';

interface EventMiniatureProps {
  
}

const EventMiniature: FC<EventMiniatureProps> = ({ }) => {
  return (
    <div>EventMiniature</div>
  );
};

export default EventMiniature;