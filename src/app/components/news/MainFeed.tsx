import React, { FC } from 'react';

interface MainFeedProps {
  
}

const MainFeed: FC<MainFeedProps> = ({ }) => {
  return (
    <div className='flex flex-col min-h-screen flex-3 bg-white'>MainFeed</div>
  );
};

export default MainFeed;