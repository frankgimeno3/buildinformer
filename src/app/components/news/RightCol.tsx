import React, { FC } from 'react';

interface RightColProps {
  
}

const RightCol: FC<RightColProps> = ({ }) => {
  return (
    <div className='min-h-sceen bg-gray-200 flex-1 hidden md:block'>
        
    </div>
  );
};

export default RightCol;