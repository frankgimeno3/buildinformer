import React, { FC } from 'react';

interface CompanyProps {
  
}

const Company: FC<CompanyProps> = ({ }) => {
  return (
    <div className='min-h-screen'>Company</div>
  );
};

export default Company;